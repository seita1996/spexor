# @spexor/db

SQLite persistence layer for Spexor.

It stores parsed spec snapshots, scenario records, and manual run history, and exposes a synchronous database API used by the app layer.

License: MIT
