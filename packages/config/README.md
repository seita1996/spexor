# @spexor/config

Configuration loader for Spexor.

It reads `spexor.config.ts`, applies defaults, resolves absolute paths, and normalizes shared-results settings for the rest of the workspace.

License: MIT
