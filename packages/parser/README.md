# @spexor/parser

Parser utilities for Spexor `.feature` files.

This package scans spec directories, reads YAML frontmatter, parses Gherkin, and returns structured feature data plus parse issues.

License: MIT
